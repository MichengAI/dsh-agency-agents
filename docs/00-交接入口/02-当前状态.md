# 当前状态

更新时间：2026-08-15 16:50（Asia/Shanghai）

阶段：可发布。

- 内置专家资产：271 名有效专家，覆盖 17 个标准分区和 1 个 `mcp-memory` 集成专家。
- 安装：已提供 DSH bundle patch，默认使用包内资产。
- 验证：`pnpm build`、`pnpm test`（17 项）和 `pnpm verify` 已通过。
- 有效风险：内置名单是上游快照；上游更新不会自动进入已发布版本，需要按来源文档同步并重新验证。
