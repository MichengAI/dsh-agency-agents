# 当前状态

更新时间：2026-08-15 20:08（Asia/Shanghai）

阶段：可安装、可发布；真实宿主委派尚未闭环。

- 内置智能体资产：271 名有效智能体，覆盖 17 个标准分区和 1 个 `mcp-memory` 集成智能体。
- 安装：已提供 DSH bundle patch，默认使用包内资产。
- 验证：`pnpm test` 33 项通过；`pnpm verify` 已锁定内置 271 和 17 个非空分区。
- 评审修复：已处理上次评审的重要代码项与遗留的 `vibe`、Unicode 截断、环境变量延迟读取和子代理 prompt 隔离；复核见 [项目代码评审](../01-迭代记录/2026-08-15-项目代码评审/迭代总览.md)。
- 有效风险：
  - 内置名单是上游快照，上游更新不会自动进入已发布版本。
  - 尚未在隔离 DSH profile 上完成一次真实召唤；`deny: list_experts` 是否被宿主 `restrict()` 接受，单测证明不了。
