# 待办与阻塞

更新时间：2026-08-15 20:32（Asia/Shanghai）

当前无阻塞事项。评审中的代码与文档项已修复并复核。

| 事项 | 负责人 | 优先级 | 下一步 | 解除条件 |
| --- | --- | --- | --- | --- |
| npm 受信发布绑定与验收 | 维护者 | 高 | 在 npm 包设置绑定 `MichengAI/dsh-agency-agents` 与 `publish.yml`，随后推送新的同版本 `v*` 标签 | GitHub Actions 测试和 npm 发布均成功 |
| 真实 DSH profile 端到端验证 | 维护者 | 高 | 在隔离 profile 安装插件并召唤一个智能体 | 配置导出和一次委派均成功，且 `deny: summon_expert,list_experts` 生效 |
| 上游 persona 更新评估 | 维护者 | 中 | 对照上游变更并同步资产 | 测试、构建、发布验证均通过 |
